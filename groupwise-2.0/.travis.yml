language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
  - pip install codecov
# command to run tests
script: 
  - python run.py
  - coverage run test_group.py

after_success:
  - codecov
